# Regenerated by setup.sh / run_all.sh (multi-GB; never committed).
# gdal/ = the PR checkout; build*/ install*/ = artifacts; fixtures/ = synthetic repos.
gdal/
build/
build-asan/
install/
install-asan/
fixtures/

# Transient
__pycache__/
*.pyc
*.pyo
.DS_Store
/tmp/
scratch/
*.tmp

# Verbose build logs (regenerated by setup.sh); the run-evidence logs are kept.
qc/asan_build.log
qc/asan_configure.log
